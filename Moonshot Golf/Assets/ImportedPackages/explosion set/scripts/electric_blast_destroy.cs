using UnityEngine;
using System.Collections;

public class electric_blast_destroy : MonoBehaviour {

    // Use this for initialization
    void Start()
    {
        Destroy(gameObject, 1.4f);
    }
}
